[pytest]
minversion = 7.0
testpaths = tests
asyncio_mode = auto

addopts =
    -ra
    --cov=utils
    --cov=services
    --cov=handlers/common
    --strict-markers
    --cov-report=xml
    --cov-report=term